# Application Configuration
NAME_APP=PersonalWeb03

# Blog Storage Path
# Absolute path on the server where blog files are stored
# Must contain a posts/ subdirectory
PATH_BLOG=/path/to/blog/storage

# Project Resources Path
# Absolute path to directory containing project resources
# Must contain a hero-section/ subdirectory with:
#   - last-7-days-activities-summary.md
#   - project_time_entries.csv
PATH_PROJECT_RESOURCES=/path/to/project/resources

# Database Configuration
# Filename of the SQLite database
NAME_DB=personalweb03.db

# Full directory path where the database file will be created/stored
PATH_DATABASE=/path/to/database/directory

# JWT Configuration
# Secret key for JWT token signing (should be a long random string)
JWT_SECRET_KEY=your-secret-key-here

# Authentication Configuration
# Comma-separated list of email addresses authorized to register
# Only emails in this list can create accounts via /auth/register
# If empty or not set, all registrations will be blocked
# Example: EMAIL_ADMIN_LIST=admin@example.com,user@example.com
EMAIL_ADMIN_LIST=your-email@example.com
